"""Request and response shapes for the HTTP layer.

Separate from `core.models` on purpose: the wire format is allowed to change for
presentation reasons without touching the domain contract that AGENTS.md froze.
"""
